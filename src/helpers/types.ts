export type notificationProps = {
    id?: number,
    title: string,
    description: string,
    photoURL: string,
    isIndividual: boolean,
    isRevised: boolean,
    iconName: string,
    iconProps?: any,
    props?: any,
};
